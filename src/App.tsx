import { Typography } from '@mui/material';

import './theme/index';

export const App = () => (
    <div>
        <Typography color="background.paper" component="h1" variant="h1">
            Hello World
        </Typography>
    </div>
);
